apply plugin: 'scala'
apply plugin: 'java'

// Properties
sourceCompatibility = 1.5
version = '1.0-SNAPSHOT'
mainClass='org.frankversnel.poortjes.game.Poortjes'
env = System.getenv()
scalaHome = env['SCALA_HOME']
libraryFolder = 'lib_managed'

defaultTasks 'clean', 'compileScala'

dependencies {
	scalaTools fileTree(dir: "$scalaHome/lib").matching { include '*.jar' }

	compile fileTree(dir: "$scalaHome/lib").matching { include 'scala-library.jar' }
	compile files(fileTree(dir: libraryFolder, includes: ['*.jar'])) 

	runtime files(fileTree(dir: libraryFolder, includes: ['*.jar'])) 
}

sourceSets {
    main {
        scala {
            srcDir 'src/main/scala'
        }
    }
}

compileScala {
  //scalaCompileOptions.useCompileDaemon = true
}

jar {
    manifest {
        attributes 'Implementation-Title': 'Poortjes', 'Implementation-Version': version
        attributes 'Main-Class': mainClass
    }
    dependsOn configurations.runtime
    from { configurations.runtime.collect { it.isDirectory() ? it : zipTree(it) } }
}

